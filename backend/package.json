{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "start": "node ./src/server.js",
    "dev": "nodemon ./src/server.js",
    "test": "NODE_ENV=test jest --forceExit --runInBand --coverage",
    "test:ci": "NODE_ENV=test BACKEND_JWT_SECRET=Test MONGOMS_VERSION=4.2.8 jest --forceExit --runInBand --coverage -ci",
    "watchtest": "NODE_ENV=test jest --forceExit --runInBand --watchAll"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "express-async-errors": "^3.1.1",
    "express-validator": "^6.14.2",
    "find-config": "^1.0.0",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.29.3",
    "mongoose": "^6.3.2",
    "shortid": "^2.2.16",
    "valid-url": "^1.0.9"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^3.0.1",
    "jest": "^28.1.1",
    "mongodb-memory-server": "^8.7.0",
    "nodemon": "^2.0.16",
    "supertest": "^6.2.3"
  },
  "jest": {
    "preset": "@shelf/jest-mongodb",
    "testTimeout": 20000,
    "setupFilesAfterEnv": [
      "<rootDir>/test/global-setup.jest.js"
    ]
  }
}
